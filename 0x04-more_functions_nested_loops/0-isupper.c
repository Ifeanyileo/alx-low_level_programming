#include "main.h"

/**
 * _isupper - functions that check for uppercase character
 * @c: the character
 * return: 1 if c is uppercase overwise 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{	
		return (1);
	}
	else
	return (0);
}
